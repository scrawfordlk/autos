[package]
name = "autos"
edition = "2024"

# Optimise, even for non-release builds
[profile.dev]
opt-level = 3
overflow-checks = false

[rust-analyzer.diagnostics.disabled]
unlinked-file = true

[dependencies]
walkdir = "2.5.0"
